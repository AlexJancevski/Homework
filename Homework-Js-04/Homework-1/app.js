function tellStory(name, mood, activity) {
    return "This is " + name + ". " + name + " is a nice person. Today they are " + mood + ". They are " + activity + " all day. The end.";
  }

  let story = tellStory("Eric", "happy", "reading");

console.log(story);